# upload_all.sh 실행 전 환경 변수 (이 파일을 .env 로 복사한 뒤 값을 채우세요)
#
# Git Bash / WSL 예:
#   cp .env.example .env
#   (에디터로 .env 편집)
#   set -a && source .env && set +a && ./upload_all.sh
#
# 한 줄로만 쓸 때:
#   export AIHUB_API_KEY='여기에_키'

AIHUB_API_KEY=
